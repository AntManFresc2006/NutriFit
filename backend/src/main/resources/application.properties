# Spring Boot backend - shared configuration
# Use the 'local' profile to load DB credentials from environment variables.
# Do NOT store secrets directly in checked-in files. See application-local.properties.

spring.main.banner-mode=off
